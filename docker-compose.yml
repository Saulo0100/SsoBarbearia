﻿version: '3.8'

services:
  api:
    container_name: barbearia-sso
    build: .
    ports:
      - "5001:80"
    restart: always
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=http://+:80